# Copy this file to `.env` in the project root and fill in your credentials.
# IMPORTANT: `.env` is ignored by git (see .gitignore). Do not commit secrets.

# Alpaca data API credentials
ALPACA_API_KEY=YOUR_API_KEY_ID
ALPACA_SECRET_KEY=YOUR_SECRET_KEY

# Alpaca feed: "iex" (most accounts) or "sip" (paid full feed)
ALPACA_FEED=iex


